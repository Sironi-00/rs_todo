pub fn _to_imp() {
    println!("    // loop >  conditionals (add, view, complete, remove, Quit)
    // adding + Store > _Index, Title, Note, _Complete
    // View > All or Each
    // Complete > toggler
    // Remove > Selected \n\n\n");
}

mod _storage {
    // when staring new - create - c
    // when starting old - read - r

    // when adding - store - u
    // when reading - read - r

    // ##############
    // Store=w at end | Reads n move data to in app > Store=w at end |
    // w | R > W 
}