mod ui;

fn main() {
    to_imp();

    ui::run();
    };
fn to_imp() {
    println!("    // loop >  conditionals (add, view, complete, remove, Quit)
    // adding + Store > _Index, Title, Note, _Complete
    // View > All or Each
    // Complete > selector + toggler
    // Remove > All or Selected \n\n\n");
};
